package org.indiafoss.companion.ui.theme

import androidx.compose.material3.Typography

/**
 * The stock Material 3 type scale (Roboto / the device's system font). No
 * display font is bundled: the native app follows the platform, and the
 * IndiaFOSS pixel face stays on the web.
 */
val CompanionTypography = Typography()
