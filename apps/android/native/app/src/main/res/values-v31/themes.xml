<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 12+ draws the launch splash itself: the themed launcher icon on
      this background. Declared so it matches the app's own surface instead of
      the platform default (#33).
    -->
    <style name="Theme.Companion" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    </style>
</resources>
