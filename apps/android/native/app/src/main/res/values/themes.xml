<resources>
    <!-- Compose owns the colours; this only styles the window before the first frame. -->
    <style name="Theme.Companion" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Shown until Compose paints, and used by the Android 12+ splash. -->
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
