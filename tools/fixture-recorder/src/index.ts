/**
 * Capture upstream responses as deterministic fixtures.
 *
 * Placeholder CLI — implementation lands in a later phase.
 */
export function main(): void {
  console.log('fixture-recorder: not implemented yet');
}

if (import.meta.url === `file://${process.argv[1]}`) {
  main();
}
